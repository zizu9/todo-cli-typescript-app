# todo-cli-typescript-app
A todo cli app using typescript

Install the dependencies using ```npm install```<br>
Compile the app using ```tsc``` inside the project directory.<br>
Run the app using ```node dist/index.js```
